# Kubeflow Trial

This repo contains notebooks that showcase below:
- Launching a kubernetes cluster locally and install kubeflow for testing the dashboard locally.
- Creating pipeline YAML specification via SDK